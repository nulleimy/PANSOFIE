{
  "name": "pansofie",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:domain": "node scripts/check-domain-model.mjs",
    "check:access": "node scripts/check-access-model.mjs",
    "check:flow": "node scripts/check-experience-flow.mjs",
    "check:pilot": "node scripts/check-pilot-kit.mjs",
    "check:partners": "node scripts/check-partner-kit.mjs",
    "check:deploy": "node scripts/check-deployment-readiness.mjs",
    "check:db-security": "node scripts/check-db-security-hardening.mjs",
    "check": "npm run check:domain && npm run check:access && npm run check:flow && npm run check:pilot && npm run check:partners && npm run check:deploy && npm run check:db-security && vite build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "@tanstack/react-query": "^5.66.8",
    "clsx": "^2.1.1",
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.1",
    "recharts": "^2.15.1",
    "tailwind-merge": "^2.6.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.1",
    "tailwindcss": "^3.4.17",
    "vite": "^5.4.14"
  }
}
